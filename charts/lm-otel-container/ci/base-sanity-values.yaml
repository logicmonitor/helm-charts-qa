portalName: "testportal"
clusterName: "ci-test-cluster"
clusterIdentity:
  mode: explicit

auth:
  existingSecret: "lm-otel-container-auth"
  # CI has no pre-created Secret; let the chart create it so pods can start.
  token: "ci-dummy-bearer-token"

ksm:
  bundled:
    enabled: true
